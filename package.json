{
    "name": "your-package",
    "description": "…",
    "version": "1.0.0",
    "scripts": {
        "test": "jest",
        "lint": "npx eslint --fix --ext .js"
    },
    "devDependencies": {
        "jest": "^29.5.0"
    }
}